abstract final class AppDebounce {
  static const search = Duration(milliseconds: 300);
  static const typing = Duration(milliseconds: 350);
  static const validation = Duration(milliseconds: 450);
  static const submit = Duration(milliseconds: 600);
  static const refresh = Duration(milliseconds: 800);
}
