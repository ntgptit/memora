// Shared repository providers belong here only when multiple features
// genuinely consume the same client-side repository instance.
//
// Keep feature-owned repository providers inside the feature package until a
// cross-feature need appears.
