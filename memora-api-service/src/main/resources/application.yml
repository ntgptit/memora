spring:
  profiles:
    default: dev
  application:
    name: memora-api-service
  messages:
    basename: messages
    encoding: UTF-8
    fallback-to-system-locale: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: memora
        jdbc:
          time_zone: UTC
  flyway:
    create-schemas: true
    default-schema: memora
    schemas: memora
    validate-migration-naming: true
    clean-disabled: true
  thymeleaf:
    check-template-location: false

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info

app:
  i18n:
    default-locale: en
    supported-locales:
      - en
      - vi
  cors:
    allowed-origins: []
    allowed-origin-patterns: []
    allowed-methods:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      - OPTIONS
    allowed-headers:
      - Authorization
      - Content-Type
      - Accept
      - Origin
      - X-Requested-With
    exposed-headers:
      - Location
    allow-credentials: true
    max-age-seconds: 3600
  security:
    public-paths:
      - /api/v1/auth/login
      - /api/v1/auth/register
      - /api/v1/auth/refresh
      - /api/v1/auth/logout
      - /api/v1/auth/forgot-password
      - /actuator/health
      - /actuator/info
    access-token-secret: ${MEMORA_ACCESS_TOKEN_SECRET:memora-dev-access-token-secret-2026-change-me}
    access-token-expires-in-seconds: ${MEMORA_ACCESS_TOKEN_EXPIRES_IN_SECONDS:28800}
    refresh-token-expires-in-seconds: ${MEMORA_REFRESH_TOKEN_EXPIRES_IN_SECONDS:2592000}
    token-issuer: ${MEMORA_TOKEN_ISSUER:memora-api-service}
  docs:
    title: Memora API Service
    description: REST API for Memora study, flashcard, and SRS workflows.
    version: v1
